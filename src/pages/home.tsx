import { NextPage } from "next";
import React from "react";
import Layout from "@/src/app/layout";

const Home: NextPage = ({}) => {
  return <Layout>Home: NextPage</Layout>;
};

export default Home;
