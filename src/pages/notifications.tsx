import { NextPage } from "next";
import React from "react";
import Layout from "@/src/app/layout";

const Notifications: NextPage = ({}) => {
  return <Layout>Notifications: NextPage</Layout>;
};

export default Notifications;
