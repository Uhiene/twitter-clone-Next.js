import { NextPage } from "next";
import React from "react";
import Layout from "@/src/app/layout";

const Search: NextPage = ({}) => {
  return <Layout>Search: NextPage</Layout>;
};

export default Search;
